<p id="notice"><%= notice %></p>

<p>
  <strong>Email:</strong>
  <%= @player.email %>
</p>

<% @player.poker_tables.each do |poker_table| %>
<div>
  	<%= poker_table.name %>
  	<%= poker_table.actual_time %>	
</div>
<% end %>

<%= link_to 'Edit', edit_player_path(@player) %> |
<%= link_to 'Back', players_path %>
